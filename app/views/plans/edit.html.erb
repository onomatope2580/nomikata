予定の編集
<%= form_with model: @plan, url: user_plan_path(id: @plan.id, date: @plan.date), method: :patch, local: true do |f| %>

  <%= f.label "スケジュール編集" %>
  <%= f.text_area :schedule %>

  <%= f.label "目標編集" %>
  <%= f.text_area :alcohol_amount_plan %>mg

  <%= f.submit "保存する" %>

<% end %>

<%= link_to "詳細ページへ戻る", user_plan_path(id: @plan.id) %>
<%= link_to "カレンダーへ戻る", user_plans_path %>